import { errorHandler } from '../../src/middlewares/errorMiddleware.js';
describe('Error Middleware', () => {
  it('should handle ApiError and log context', () => {
    // ...existing code...
    expect(true).toBe(true);
  });
});
